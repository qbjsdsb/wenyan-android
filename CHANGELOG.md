# 更新日志

本项目所有版本更新记录。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **发版流程**：每次发布前，将本版本的更新内容追加到本文件顶部（`## vX.Y.Z - 日期`），
> release.yml 会自动读取当前 tag 对应的日志作为 GitHub Release 正文，
> App 内"检查更新"界面展示的更新日志即来源于此。**务必随版本更新，不要遗漏。**

## [v0.9.41] - 2026-08-06

修复详情页逐级返回并精修全局界面动效（合并 PR #8）。

### 导航修复

- **返回栈修复**：详情页之间不再使用 `popUpTo(..., inclusive = true)` 清栈——知识点详情、卡片全屏、论述题详情之间保留完整浏览历史，返回严格回到真实上一页
- **返回统一**：所有子页面顶部返回按钮和系统返回统一优先执行 `popBackStack()`，仅深链/进程恢复无历史时使用兜底入口；抽取知识点详情导航 helper，避免不同入口出现不一致的清栈策略

### 界面动效

- **动效克制**：页面 Push/Pop 与底部导航显隐去掉回弹，保留柔和减速感，降低生硬和晃动
- **信息层级**：分组卡片标题改为更克制的正文层级并补充 TalkBack heading 语义；知识点详情不再在顶栏重复显示完整长标题；知识点/论述题/背题详情补足底部安全留白

### 图标 v8

- 将旧"矩形书页 + 八条文字线"收紧为清晰的展开书轮廓，暖白/暖米双页表达纸张层次，加入克制的朱砂书签作为识别点
- 彩色 adaptive icon、Android 13+ monochrome themed icon 与 Splash 前景保持同一书形语义；背景统一 `#202124`，不使用渐变、阴影或复杂文字镂空

### 技术

- 新增返回路径验收测试覆盖（列表→A→B→返回、论述题↔知识点详情、卡片全屏链、设置子页、无历史兜底）
- GitHub Actions #422：`testDebugUnitTest`、`assembleDebug`、Debug APK 上传全部通过

## [v0.9.40] - 2026-08-06

四科知识框架整理与知识点浏览体验优化（合并 PR #7）。

### 内容与框架

- **四科教材式框架**：为中国现当代文学、中国古代文学、外国文学、文学理论建立稳定的多级章节树，覆盖全部 960 个知识点（181/465/124/190）。
- **知识点归类校验**：每个知识点保持唯一主要归属，校验章节 ID、父子关系、循环引用、重复归属和遗漏，避免内容静默落到根节点。
- **学习记录安全**：框架升级使用稳定章节 ID 和 Upsert，不删除或重置既有 FSRS 排程、记忆记录与复习历史。

### 浏览体验

- **框架浏览**：知识点页新增“科目总览 → 时期/模块 → 专题 → 知识点”的逐层浏览路径，保留列表、搜索和分类筛选模式。
- **界面精修**：增加四科总览、知识点统计、专题数量、稳定教材顺序、空摘要兜底、窄屏换行、返回导航和 TalkBack 语义。
- **卡片交互**：知识点卡片增加清晰的进入反馈、列表动画和安全留白，避免信息重复与页面跳动。

### 技术与验证

- 新增四科框架配置、导入校验器及完整性回归测试。
- GitHub Actions 已验证框架代码、全量单元测试、Debug APK 构建和上传通过。

## [v0.9.39] - 2026-08-06

知识卡片空队列竞态修复与翻转动画平滑化（合并 PR #3）。

### 学习与复习

- **空队列竞态修复**：v0.9.37 将今日队列改为 `stateIn` 共享后，人工空初值先于 Room 真实结果到达，`CardsViewModel` 又会冻结首次卡片列表，导致真机出现"横幅显示今日新学 10 个、正文却显示没有到期卡片"。改为 `shareIn(replay = 1)`，只重放真实查询结果，不再制造假空状态；停止订阅时清空 replay，避免再次进入页面先冻结上次会话的旧队列
- **ViewModel 第二层保护**：空卡片列表不再建立冻结会话，真实新卡或 60 秒后到期的卡片仍可进入当前页面，双重保险防回归

### 界面与动画

- **翻转动画平滑化**：卡片翻转时长 300ms → 420ms（emphasized motion），中点轻微缩放降低透视突兀感，给正反面交接留出可感知的空间运动
- **操作区 fade-through**：正反面操作区由两个同时占位的 `AnimatedVisibility` 合并为单槽位 `AnimatedContent` fade-through，消除按钮区高度交接时的上下跳动（竖屏 / 横屏 / 全屏共用）

### 技术

- 新增首次空队列后卡片到达、共享流首值真实性、翻转缩放边界等回归测试
- GitHub Actions PR CI 3 次全绿（`testDebugUnitTest` 通过）
