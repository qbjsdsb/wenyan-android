# 更新日志

本项目所有版本更新记录。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **发版流程**：每次发布前，将本版本的更新内容追加到本文件顶部（`## vX.Y.Z - 日期`），
> release.yml 会自动读取当前 tag 对应的日志作为 GitHub Release 正文，
> App 内"检查更新"界面展示的更新日志即来源于此。**务必随版本更新，不要遗漏。**

## [v0.9.36] - 2026-08-05

知识卡片全屏沉浸模式（项目首个沉浸式先例）。

### 全屏沉浸模式

- **顶栏全屏入口**：卡片页有卡可复习时顶栏出现全屏按钮（加载/错误/空/完成态自动隐藏），点击 Push 进入全屏沉浸页
- **隐藏系统栏**：状态栏 + 导航栏隐藏，内容占满全屏（滑动屏幕边缘可临时唤出，自动隐藏）
- **浮动退出按钮**：左上角半透明圆形 ×，随时退出全屏回到卡片页（无系统栏返回，出口明确）
- **共享复习会话**：全屏页与卡片页共享同一 ViewModel（hiltViewModel getBackStackEntry），当前卡片/翻转状态/会话统计实时同步，退出后无缝衔接
- **横屏双栏变体**：卡片放宽 560dp（全屏高度更大，比例 ~1.45:1 仍协调）+ 右操作栏 280dp 单列竖排评分按钮（用户"一个个竖着排列"偏好）
- **竖屏放宽上限**：无顶栏/横幅释放空间后卡片最大宽度放宽（compact→comfortable）
- **全功能镜像**：Leech 警告对话框（问 AI / 查看知识点）/ 加入错题本 / Snackbar / 撤销跳过，与卡片页行为一致

### 技术

- 新增 `ImmersiveSystemBars`（WindowInsetsControllerCompat + BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE），离开组合自动恢复系统栏
- 全屏横屏布局回归测试 4 个：卡片 560dp 居中 / 操作栏 280dp / 评分按钮单列竖排 / 面板垂直居中
- 全量 **579 单测 0 失败** + assembleDebug 通过

## [v0.9.35] - 2026-08-05

横屏协调优化 + 全面质量审计修复（18 项）。

### 横屏知识卡片协调优化

- **卡片限宽 480dp 居中**：实测横屏卡片 584dp 宽（比例 1.73:1）文本行过长似横幅，限宽后比例 ~1.42:1 阅读舒适；进度条/新卡徽章同步对齐居中
- **操作面板垂直居中**：右栏评分按钮组不再"悬空"在顶部（原下方 160~240dp 空白），与左侧卡片垂直平衡
- **横屏协调性回归测试**：卡片 480dp 居中 / 面板垂直居中 / 2×2 评分网格位置 / 矮屏超高可滚动，共 5 个断言，布局改动破坏协调性自动预警

### 全面质量审计修复（18 项）

- **双断点不一致**：横屏手机（窗口 600-679dp）被侧边导航占宽后内容区 <600dp，双栏永不激活 → 补窗口宽度类判定
- **窄横屏顶栏过高**：高度 <480dp 一律降级标准顶栏（原大标题 152dp 吃光内容区）
- **背题快速连点竞态**："不会"连点会重复写入错题本（错答次数虚增）+ 静默跳题 → 同步推进 + 400ms 防连击
- **新卡排序方向错误**：同考频下原取最旧知识点，新内容长期不出现 → 改为新内容优先
- **考试日期两套真相**：设置页倒计时不联动用户设置的日期 → 注入联动 + 跨午夜自动刷新
- **AI 消息重复注入**：本次提问在 LLM 上下文中出现两次（浪费 token 误导模型）
- **AI 幽灵回复竞态**：清空/新建对话后被取消任务的"半截回复"重新冒出 → 对话代次防护
- **多轮上下文 token 预算**：长对话可超 8k 模型限制（400 错误）→ 按预算从最早消息截断
- 其他：错题递增清调度时间 / 调度失败可重试 / 背题参数空安全 / UI 与 VM 的 https 校验同步 / 学习时长统计打通 / 4 处横屏限宽贴左 / 空错态全宽 / 文案资源化等

### 技术

- 全量 **574 单测 0 失败** + assembleDebug 通过

## [v0.9.34] - 2026-08-05

全局横屏适配：知识卡片双栏布局 + 全 App 横屏巡检。

### 知识卡片（横屏核心体验）

- **横屏双栏布局**：复习页横屏时从单栏 Column 改为"左卡片 / 右操作面板"——卡片占全部高度 + 大部分宽度（突出、大、方便阅读），评分按钮收敛到右侧 200dp 窄列
- **评分按钮 2×2 网格**：横屏每按钮 ~90dp（原横排 ~136dp）、总高 ~120dp（原 ~184dp）——按钮更小更紧凑，不溢出矮横屏
- **智能判定**：`shouldUseDualPane`（宽 > 高且宽 ≥ 600dp）精确捕获横屏手机，不误触发平板竖屏；竖屏布局零改动
- **今日任务横幅横屏紧凑化**：降级为单行核心任务信息，释放 ~70dp 给卡片
- 翻转/切卡后操作面板滚动自动回到顶部（评分按钮始终优先可见）；矮屏右栏滚动兜底

### 全局横屏巡检

- **列表类 Screen**（知识点/论述题/真题背题/错题本）：搜索/筛选栏横屏不再全宽拉伸，与列表对齐限宽居中
- **AI 助手**：输入栏横屏限宽居中（IME 独占语义不变）
- **背题详情**：底部操作栏与题干对齐限宽居中
- **会话完成态**：横屏限宽防止统计卡过宽拉伸

### 技术

- 新增 `AdaptiveWindowLayout` 设计系统组件（BoxWithConstraints 暴露内容区尺寸 + 可单测判定函数）
- 新增横屏 @Preview（Cards - Landscape Front/Back）
- 全量 **569 单测 0 失败**（+10 横屏判定边界测试）+ assembleDebug 通过

## [v0.9.33] - 2026-08-04

真题背题专项：名词解释/简答背诵模式。

### 真题背题（知识点 Tab 新增入口卡）

- **入口**：知识点页搜索框上方新增"真题背题"入口卡（副标题明确"名词解释 · 简答专项，练'记得住'（论述题见论述题 Tab）"），与论述题板块划清边界——背题练记忆、论述题练写作
- **列表页**：题型（全部/名词解释/简答）+ 科目 + 年份三维筛选，实时显示"共 N 题 / X / Y 题"；卡片展示题型标签（底色区分）+ 科目 + 年份 + 答案字数 + 题干预览
- **详情页（纯背诵模式）**：题干大字展示 → 点"显示答案"展开结构化参考答案 → 对照记忆后标记"会了"（直接推进）/"不会"（写入错题本并自动进入 FSRS 复习队列）；上一题/下一题在同一筛选集内导航，进度"第 X / N 题"
- **数据层**：`ExamQuestionDao.observeByQuestionTypes` 多题型 IN 查询（稳定 ORDER BY：year DESC + exam_paper_code + id，保证前后题导航顺序不漂移）；`KnowledgeRepository.observePracticeQuestions` 题型白名单封装，从数据层排除 ESSAY 避免与论述题 Tab 重复展示
- **错题本联动**：标记"不会"→ `recordWrongAnswer(SOURCE_QUIZ_WRONG)`，错题本显示"真题练习"来源标签，重复标记自动累计错答次数不新增记录

### 工程质量

- Snackbar `withTimeout(5s)` 防挂起：对齐 CardsScreen v0.9.23 / WrongAnswerScreen v0.9.25 修复模式（material3 1.5.0-alpha18 计时异常会导致 showSnackbar 永久挂起、状态清不掉）
- 详情页错误态重试按钮可用：`retry()` 取消旧加载 job + 重新加载，`CancellationException` 正确 rethrow 防止取消误入错误态
- 修复"不会"写入失败时成功文案覆盖失败文案的误导提示

### 技术

- 新增 4 文件（列表/详情 Screen + ViewModel）+ DAO/Repository/导航/入口/i18n（28 条新字符串）
- 全量 **559 单测 0 失败** + assembleDebug 通过

## [v0.9.32] - 2026-08-04

AI 界面修复 + 功能完善 + 批次 D 合规。

### AI 界面修复

- **输入框上方大面积空白（P0 修复）**：根因是 IME 双重消费——Scaffold 默认 `contentWindowInsets` 已含键盘 insets，输入栏又自行 `.imePadding()`，键盘弹出时内容区底部多出键盘高度空白。现由输入栏独占 IME 处理（`contentWindowInsets=0`），键盘弹出后输入框紧贴键盘、消息区与输入框无缝衔接

### AI 功能完善

- **键盘 Enter 直接发送**：输入框回车即发送（聊天应用惯例），无需再点发送按钮
- **空状态学习问题建议**：新对话时展示 4 个学习问题卡片（现代文学脉络/文以载道/红楼梦艺术成就/呐喊核心思想），点击一键向 AI 提问，新用户上手零门槛

### 批次 D 合规

- **validateBaseUrl 强制 https**：拒绝 `http://` 明文敞口（网络层本就禁明文，此前放行导致"保存成功→AI 调用神秘失败"），明确提示改用 https；单测捕获并修复 `https:///` 无域名漏判

### i18n

- AI 模块补全 6 处残留硬编码（空状态提示/学习工具 4 标题/token 用量/引用来源/横幅按钮），剩余硬编码仅枚举展示名（政策豁免）

### 技术

- 全量 **559 单测 0 失败** + assembleDebug/assembleRelease(R8) 通过

## [v0.9.31] - 2026-08-04

知识卡片学习科学三改进 + 整体布局精修 + 评分按钮统一。

### 知识卡片学习科学

- **今日任务横幅按知识点显示**：修复"过 1 张卡复习数减 6"的困惑——横幅现在显示"今日：新学 N 个知识点 · 复习 M 个知识点"，与每日新卡上限（张）清晰区分
- **新卡学习步（Anki 最佳实践）**：新卡首次评分 GOOD 先进 10 分钟学习步再毕业（LEARNING→REVIEW 三阶段），EASY 保留直接毕业；缓解"新卡立刻变长间隔"的遗忘风险
- **新卡徽章**：卡面显示"新卡 · 首次学习（10 分钟后强化一次）"徽章，明确当前处于首次学习阶段

### UI/UX 优化（第二轮布局精修 + 评分按钮统一）

- **评分按钮三处重复统一为设计系统公共组件** `WenyanRatingButton`：卡片复习（四档+预期间隔）/ 错题本（四档）/ 论述题自评（三档选择）共用同一实现，消除 135 行重复
- **论述题自评补评分色编码**：选中"不会"=红、"尚可"=绿、"轻松"=蓝，与卡片/错题本颜色直觉统一（此前自评三档全部中性色无区分）
- **大屏宽度统一**：裸 600dp 限制收敛为 `MaxContentWidth.compact` + 居中 Box（平板/横屏阅读更舒适）
- **触控目标补全**：自评按钮 48dp；撤销按钮恒占位（透明禁用）消除布局跳动
- **间距呼吸感**：FlipCard 翻转高度平滑过渡（`animateContentSize`）、PerspectiveCard 内边距优化

### 技术

- `WenyanRatingButton` 双模式 API：动作模式（isPrimary→filled/tonal + 四档颜色）/ 选择模式（isSelected→FilledTonal/Outlined 叠加评分色）；统一 `heightIn(min=48dp)` 满足 M3 触控规范
- 全量 **551 单测 0 失败**

## [v0.9.30] - 2026-08-04

知识卡片打磨 + UI/UX 全面优化 + i18n 资源化 + 仓库卫生。

### 知识卡片打磨

- **复习/新卡比例保护**：今日复习知识点较多时自动减少/暂停新卡（复习 ≤10 点新卡全量、11-20 减半、>20 暂停），避免"复习 + 新卡"叠加堆积
- **今日任务显示优化**："约 X 张（N 个知识点）"更诚实；无复习时只显示新卡

### UI/UX 优化（批次 C，14 项）

- **AI 助手**：光标"▍"闪烁动画（此前静态）；移除幽灵头像 48dp 留白；停止按钮改方块图标（语义正确）；错误提示改长时长不被截断
- **触控目标统一 ≥48dp**：错题本/论述题详情/设置科目行/ApiConfig 表单按钮
- **布局稳定**：错题本过滤 chips 改 FlowRow（窄屏不溢出）；论述题筛选 chip 常驻图标（消除重排跳动）；卡片撤销按钮恒占位（消除按钮切换跳动）；卡片翻转高度平滑过渡
- **空 item 条件化**：知识点详情 4 个区块空内容不再残留空白
- **更新检查页**：TopBar 与其他页统一（滚动收缩）
- **ApiConfig 必填校验**：空名称/地址/密钥/模型禁用保存；"0."输入中间态不再误报错
- **弱断言测试加强**：离线提示/网络错误改精确断言

### i18n 资源化（约 130 个字符串资源）

- 5 个 feature 模块 UI 文本全部资源化（考频/标题/按钮/表单/计数/时间提示）
- 修复考频 3 处重复定义；`semantics` 非 Composable 场景正确取资源
- main 源码剩余 `Text("中文")` 硬编码 = 0

### 仓库卫生（批次 B 部分）

- release-assets 74MB 旧 APK 出库（.gitignore，CI 自动生成）
- 删除临时文件与 Quiz 死代码（685 行）

### 技术

- 新增 4 个比例保护单测；全量 **551 单测 0 失败**

## [v0.9.29] - 2026-08-04

卡片备考系统——把 6000+ 张卡片拆解为每日可控的学习量。

### 新增

- **每日新卡限额（默认 60 张/天，可设 10-200）**：每天最多学习 60 张新卡（约 10 个知识点），不再一次性面对全部卡片；超过限额的新卡自动顺延，FSRS 负责排期
- **今日任务横幅**：卡片页顶部显示"距考试 N 天 · 今日新卡 X 张 · 复习 Y 张 · 学习进度条"，心里有数不焦虑
- **考频筛选**：仅高频 / 高频+中频 / 全部 三档（默认高频+中频，聚焦考试重点）
- **复习科目筛选**：古代/现当代/外国/理论 四科多选，可单独攻坚一科
- **考试倒计时计划**：设置考试日期后自动计算剩余天数与进度（已学/总知识点）

### 技术

- `CardSettingsRepository`（DataStore）：每日限额/考频/科目/考试日期持久化，非法值容错降级
- `ReviewRepository.getTodayStudyQueue`：到期复习 ∪ 每日新卡（VERIFIED+未学+考频/科目筛选，考频 HIGH 优先，按卡片数限额取整知识点）
- `getStudyProgress` / `daysUntilExam`：学习进度与倒计时计算
- 新增 27 个单测（设置仓库 9 + 新卡选择 13 + 倒计时 5）；全量 **556 单测 0 失败**

## [v0.9.28] - 2026-08-04

修复软件内"检查更新"下载安装失败（安装时提示"应用文件存在问题"）+ 知识卡片拆分质量。

### 修复（P1 hotfix）

- **软件内更新下载失败**：国内网络下 `api.github.com` 不可达时 App 走降级路径，旧逻辑把下载地址指向了 Release **网页页面（HTML）**而非 APK 文件——App 下载了一个网页当安装包，安装器自然报"应用文件存在问题"。现已修复：降级路径按 `releases/download/vX.Y.Z/wenyan-vX.Y.Z.apk` 固定命名规则直接构造**真实 APK 下载地址**，不再指向网页
- **下载完整性校验**：下载完成后对比 `Content-Length` 实际字节数，并校验 sha256（GitHub API 资产 digest）——不完整/被篡改的安装包会被丢弃并提示重试，不再把损坏文件交给安装器
- **下载失败自动重试 1 次**：国内网络单次中断会自动重新下载，减少手动重试

### 修复（知识卡片拆分质量）

- **部分知识点只拆 1 张超长卡**：960 个知识点里有 35 个因全文仅一处"标签："（如"不同：""特色："）被误判为结构化文本，整段 200-1000 字压成 1 张卡，违背最小信息原则。现已修复：结构化标签命中数 ≥3 才按维度拆卡，不足时按句末标点拆分——这 35 个知识点从 1 张卡变为 4-6 张卡（如"古代神话的产生与意义"从 1 张 279 字 → 6 张按句子拆分的卡）

### 技术

- `UpdateRepository`：新增 `resolveDownloadUrl` / `buildApkDownloadUrl`（internal，可测）；GitHubAsset 解析 `digest` 字段
- `UpdateViewModel`：下载链路加 Content-Length + sha256 双重校验、失败重试 1 次
- `CardSplitter`：新增 `MIN_STRUCTURED_DIMENSIONS=3` 阈值，结构化标签不足时回退句子拆分
- 新增 10 个下载 URL 单测 + 1 个全量卡片质量检查测试（960 知识点逐张检查 front/back）；全量 **529 单测 0 失败**

## [v0.9.27] - 2026-08-04

启动图标 v7.5 精进 + 全面检查 P1 修复 + 四科内容补齐 25 个知识点。

### 新增

- **启动图标 v7.5 精进**：在 v7.4「黑底白书」基础上手工打磨——右页米色 #F2E9D8 双色页、页脚双色厚度、右页首行缩进/末行短收（模拟真实排版）；Android 13+ 主题图标改为 evenOdd 镂空文字线，纯色单层也能清晰呈现书页结构

### 修复（全面检查 P1-1/2）

- **AI 停止/重发竞态**：AI 助手停止后立即重发时，旧任务不再清掉新任务的引用（此前 finally 无条件清空 aiJob，快速重发可能把新任务当旧任务取消）
- **AI 限流等待上限**：服务商返回的 `Retry-After` 限流秒数现在封顶 5 秒（此前不设上限，极端情况下可能长时间阻塞网络线程并占住 AI 并发槽位）

### 内容（四科补齐 25 个，seed 2.16.0 → 2.18.0，935 → 960）

- **现当代真题硬缺口（11 个）**：史铁生 / 学衡派 / 巴金《寒夜》 / 茅盾《蚀》三部曲 / 《芙蓉镇》 / 《男人的一半是女人》 / 《神鞭》《那五》 / 《现代》杂志 / 王国维《观堂集林》 / 希腊精神与希伯来精神 / 杨朔模式
- **现当代教材缺口（9 个）**：艾青 / 山药蛋派 / 荷花淀派 / 解放区文学 / 重写文学史 / 探索戏剧 / 茅盾文艺思想 / 鸳鸯蝴蝶派 / 丁帆新文学史观与1912分期
- **台港澳板块（4 个）**：台湾文学概述 / 白先勇 / 香港文学概述 / 金庸与武侠文学（对齐丁帆《中国新文学史》含台港澳的架构）
- **古代补充（1 个）**：敦煌变文（袁行霈《中国文学史》）
- 新增知识点全部按教材/真题三路交叉验证撰写，考频标注 HIGH/MEDIUM/LOW，实体关系图谱与全库格式一致

### 技术

- 内容补齐验证：960 条数据一致性校验通过 + 518 单测 0 失败 + assembleDebug 通过 + APK 内 seed 2.18.0/960 抽查确认
- 图标 v7.5：VectorDrawable path 精修（fillColor 双色页 + strokeAlpha 页脚 + evenOdd 镂空），monochrome 主题图标同步

## [v0.9.26] - 2026-08-03

新启动图标（Google Play Books 风格）+ 批三（性能与整洁）。

### 新增

- **全新启动图标（v7.4 Google Play Books 风格）**：手工矢量「黑底白书」——白色双页书 + 中央书脊缝隙 + 每页 4 条文字线，墨黑 #1A1A1A 背景；纯 VectorDrawable 可无限缩放，资源仅 ~9.8KB（此前位图版 84KB）

### 性能与整洁（批三）

- **详情页懒加载**：知识点详情页 Column+verticalScroll → LazyColumn（全部 section 分 item 懒加载，长详情页不一次布局全部）
- **RAG 检索质量**：检索过滤 `ocr_status='VERIFIED'`（未校对知识点不进 AI 上下文，与用户搜索一致）
- **AI 成本控制**：
  - 429/5xx 时读取 `Retry-After` 头（服务商告知限流秒数，按等待避免再次撞限流）
  - OkHttp `callTimeout(90s)` 兜底（含 SSE 长连接），防流式卡死空耗 token
  - AI 全局并发限制（Semaphore 3，跨页面共享，防多页面同时打 API 撞限流）
- **i18n 资源化**：核心 UI 文本（约 74 处）硬编码 → stringResource（5 个 feature 模块），为多语言/维护做准备
- **convention plugin 抽取**：新增 build-logic + android-library-convention，11 个库模块共用 compileSdk/minSdk/compileOptions 等配置（约 -130 行重复）

### 技术

- 新图标：手工矢量 path（白书 + 文字线）+ cairosvg 渲染各密度 PNG 兜底 + adaptive icon 三层
- RAG 停用词剔除尝试后回退（LIKE 匹配语义，多关键词 OR 留待后续）
- 验证：518 单测 0 失败 + assembleDebug/Release(R8) 通过

## [v0.9.25] - 2026-08-03

全新启动图标（AI 生成「书堆 + 文」）+ 整体界面审查修复。

### 新增

- **全新启动图标**：AI 生成「书堆 + 文」插画图标（毛笔 + 朱红书签点缀），替换旧几何矢量图标；自适应图标三层齐全（墨黑背景 / 前景 / Android 13+ 主题图标），旧设备与 Splash 屏同步更新

### 修复（整体界面审查）

- **AI 停止生成保留内容**：点击停止后已生成的部分回复现在会保留为一条消息（此前直接消失）
- **AI 流式自动滚动**：AI 逐字回复时列表跟随滚动到底部（此前长回复在可视区外增长看不到）
- **AI 流式转圈**：流式输出时不再同时显示加载转圈
- **状态栏图标颜色**：手动切换深色/浅色主题后状态栏图标自动跟随（此前手动深色 + 系统浅色时图标看不见）
- **更新安装**：「安装更新」现在直接安装已下载的 APK（此前会重新联网下载，离线无法安装）
- **更新日志过渡**：检查更新页状态切换动画正确显示过渡（此前旧状态帧显示新内容，crossfade 失效）
- **重试反馈**：论述题列表重试加载时显示 loading（此前点击无反馈直到数据到达）
- **错误态交互**：知识点/论述题页加载失败时禁用搜索框与筛选（此前可点击但不会重载，误导）
- **长标题截断**：论述题标题过长时最多显示 2 行省略（此前单条撑高卡片）
- **种子色暗色适配**：设置页种子色图标在暗色/AMOLED 下自动亮化（此前深色图标看不清）
- **卡片滚动重置**：知识卡片点「跳过」切新卡时滚动回到顶部（此前从上一张滚过的位置开始）
- **错题本 Snackbar**：错误提示最多显示 5 秒（此前可能常驻不消失）
- **错题本日期行**：下次复习日期窄屏/大字号下不再换行错乱（单行省略）
- **底栏跨 Tab 状态**：在一个 Tab 下滑隐藏底栏后切换 Tab，底栏恢复显示（此前保持隐藏）

### 技术

- 图标生成管线：ImageGen 候选 → PIL 抠背景/去水印/安全区居中 → 5 密度 webp（共 84KB）
- 统一 Kotlin JVM target 17（与 CI temurin JDK 17 对齐，本机 JDK 20 也可构建）
- 验证：518 单测 0 失败 + assembleDebug/Release(R8) 通过

## [v0.9.24] - 2026-08-03

AI 体验升级（批一）+ 工程质量加固（批二）。

### 新增

- **AI 真·流式输出**：AI 回复改为逐字显示（OkHttp 原生 SSE 逐行解析，零新依赖），告别"转圈等待 60 秒"
- **停止生成**：AI 回复中可点击停止，已生成的部分内容自动保留
- **多轮对话上下文**：AI 记住最近 20 条对话（约 10 轮），连续追问不再像新对话
- **Token 用量统计**：每条 AI 回复下方显示消耗 token 数
- **数据库迁移测试**：新增 MigrationTestHelper 测试（覆盖 8→9、9→10 迁移，索引缺失自动化兜底）
- **R8 混淆**：release 构建启用混淆压缩，APK 从约 26.7MB 缩至约 5.6MB（-79%），防反编译

### 修复

- **Tab 返回闪烁**：论述题列表/API 配置/学习进度页离开 Tab 超 5 秒返回不再先闪 loading（stateIn 改 Eagerly）
- **筛选查询性能**：为 exam_questions 补 question_type/answer_status 索引、knowledge_points 补 content_source 索引（数据库 9→10 迁移）
- **聊天计数一致性**：appendMessage 的消息插入 + 会话计数合并为单个事务，避免异常时计数偏差

### 技术

- 新增 `chatResultStream` 流式接口（保留旧接口兼容苏格拉底引导/回忆检测）
- 新增 `AiStreamEvent.Delta/Complete` 流式事件模型
- 新增 mockwebserver 流式 SSE 测试 + ViewModel 流式/多轮/token/停止回归测试

## [v0.9.23] - 2026-08-02

论述题体验优化 + AI 功能审计修复 + 更新日志机制落地。

### 变更

- **论述题删除年份**：列表卡片、详情页、年份筛选、知识点详情"相关论述题"区块全部移除年份显示；筛选栏只保留科目 + 审题思路（数据层 year 字段保留）
- **更新日志机制**：新增 CHANGELOG.md 按版本维护更新日志，发布时 release.yml 自动读取并写入 GitHub Release 正文，App 内"检查更新"界面展示的日志随版本变化

### 修复

- **Snackbar 常驻**：修复"加入错题本"通知一直不消失的问题（先清状态再显示 + 超时兜底，保证最多显示 5 秒）
- **AI 预设服务商 URL 拼接 bug**：修复通义/智谱/月之暗面预设配置开箱即用必 404 的问题（接口路径与 baseUrl 版本段重复）
- **AI 助手竞态**：修复发送中清空/新建对话导致消息丢失、进程重启恢复覆盖用户新消息的问题（统一任务管理 + 取消在途任务）
- **AI 并发控制**：AI 回复中重复触发学习工具被正确忽略（防重入）
- **RAG 降级**：资料库检索失败不再阻断 AI 回答（降级为无上下文调用）
- **提示词注入封堵**：苏格拉底引导阶段 2/3 的上一阶段输出加边界标记与注入警告

## [v0.9.22] - 2026-08-02

深度代码审查后的改进批 A + B（全仓库 3 路并行审查 + 关键问题人工复验）。

### 修复

- **底栏 double inset**：修复 COMPACT 布局底部手势区被双重消费导致的"底栏上方大面积空白"（外层只 pad 80dp，手势区交由内层 Scaffold 消费，与顶部 statusBars 处理对称）
- **主题保存错误提示**：设置页消费主题保存失败事件并弹 Snackbar（此前 DataStore 异常被静默丢弃）
- **FSRS 除零防御**：修复老数据 stability=0 时 `nextRecallStability` 产生 NaN 污染调度的问题
- **数据库 8→9 迁移**：为存量用户补建错题本两个复合索引（`[point_id, source]` / `[exam_question_id, source]`），提升错题查询性能
- **错题并发修复**：`recordWrongAnswer` 查找+递增/插入合并为单个事务，杜绝并发重复插入
- **时间源对齐**：错题记录改用 ClockGuard 时间源（与 FSRS 调度一致，避免时钟回拨时错乱）
- **评分防重入**：错题本 DUE 模式评分加防重入锁，避免连点导致 FSRS 重复调度

## [v0.9.21] - 2026-08-02

### 修复

- **MD3 规范底栏回归 + inset 双重消费修复**：底栏改为标准 Material 3 风格（surfaceContainer 实色 + 80dp + secondaryContainer 指示器），修复底栏/顶栏 inset 双重消费导致的布局空白
- **发布流程加固**：release.yml 支持同 tag 重建覆盖（`update_release: true`），修复 v0.9.20 已发布旧代码无法更新的问题

## [v0.9.20] - 2026-08-02

### 变更

- **底栏回归规范 MD3 风格**：从"流体玻璃/毛玻璃"回归标准 Material 3（surfaceContainer 实色 + 80dp + secondaryContainer 指示器），保留 scroll-aware 滚动感知显隐
- **滚动感知导航栏**：5 个顶级 Tab 支持下滑隐藏/上滑显示导航栏（spring 动画）

> 注：本版本因发布流程问题，实际发布的 APK 未包含随后的 inset 修复（修复已在 v0.9.21 完整发布）。

## [v0.9.19] - 2026-08-01

### 修复

- **紧凑玻璃导航栏**：悬浮导航栏高度 80dp→56dp，圆角 24dp，半透明玻璃质感，移除 BottomGradientScrim，解决导航栏+渐变遮罩合计约 184dp 占用过高的问题
- **种子加载超时重试**：超时 120s→300s + 1 次重试，防止低端设备首次加载超时导致知识点数据丢失

## [v0.9.18] - 2026-07-27

### 新增

- **错题本手动加入**：知识卡片可手动加入错题本
- **App 启动图标 v3**：印章文风格（米色印面 + 墨黑"文"字 + 圆角 12dp + monochrome 适配 Android 13+）

### 修复

- **日志门面统一**：20+ 文件迁移到 Timber 统一日志

## [v0.9.17] - 2026-07-27

### 修复

- **题号前缀剥离**：集中清洗所有题目内容的阿拉伯数字/中文数字题号前缀（6 个 UI 展示点统一处理）

## [v0.9.16] - 2026-07-27

### 变更

- **真题→论述题迁移**：底部导航第 2 个 Tab 从"真题"替换为"论述题"

## [v0.9.10] - 2026-07-31

### 修复

- **全面内容审计**：935 知识点 + 134 论述题内容完整性与学术准确性审计（0 个内容问题）
- 论述题详情分值显示修复、versionName 对齐

## [v0.9.9] - 2026-07-30

### 新增

- **论述题 AI 审题助手**：三阶段苏格拉底式引导（论证分析 → 改进建议 → 参考范文）+ 自评回写错题本

## [v0.9.8] - 2026-07-30

### 新增

- **论述题板块**：134 题三维筛选（年份/科目/审题思路）+ 详情页 10 区块结构 + 知识点关联

## [v0.9.5] - 2026-07-29

### 新增

- **时钟守卫**：检测系统时钟回拨，避免 FSRS 调度异常

## [v0.9.4] - 2026-07-29

### 新增

- **错题本 FSRS 调度**：错题支持间隔重复复习（10 个调度字段 + 待复习列表）

## [v0.9.3] - 2026-07-29

### 移除

- **知识图谱功能**：图谱 UI 及 core 层图谱设施移除，改走知识点树结构 + 关联模块

## [v0.9.0] - 2026-07-28

### 新增

- **知识图谱功能**（后于 v0.9.3 移除）
