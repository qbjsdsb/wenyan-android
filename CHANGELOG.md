# 更新日志

本项目所有版本更新记录。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **发版流程**：每次发布前，将本版本的更新内容追加到本文件顶部（`## vX.Y.Z - 日期`），
> release.yml 会自动读取当前 tag 对应的日志作为 GitHub Release 正文，
> App 内"检查更新"界面展示的更新日志即来源于此。**务必随版本更新，不要遗漏。**

## [v0.9.24] - 2026-08-03

AI 体验升级（批一：流式输出 + 停止生成 + 多轮上下文 + token 用量统计）。

### 新增

- **AI 真·流式输出**：AI 回复改为逐字显示（OkHttp 原生 SSE 逐行解析，零新依赖），告别"转圈等待 60 秒"
- **停止生成**：AI 回复中可点击停止，已生成的部分内容自动保留
- **多轮对话上下文**：AI 记住最近 20 条对话（约 10 轮），连续追问不再像新对话
- **Token 用量统计**：每条 AI 回复下方显示消耗 token 数

### 技术

- 新增 `chatResultStream` 流式接口（保留旧接口兼容苏格拉底引导/回忆检测）
- 新增 `AiStreamEvent.Delta/Complete` 流式事件模型
- 新增 mockwebserver 流式 SSE 测试 + ViewModel 流式/多轮/token/停止回归测试

## [v0.9.23] - 2026-08-02

论述题体验优化 + AI 功能审计修复 + 更新日志机制落地。

### 变更

- **论述题删除年份**：列表卡片、详情页、年份筛选、知识点详情"相关论述题"区块全部移除年份显示；筛选栏只保留科目 + 审题思路（数据层 year 字段保留）
- **更新日志机制**：新增 CHANGELOG.md 按版本维护更新日志，发布时 release.yml 自动读取并写入 GitHub Release 正文，App 内"检查更新"界面展示的日志随版本变化

### 修复

- **Snackbar 常驻**：修复"加入错题本"通知一直不消失的问题（先清状态再显示 + 超时兜底，保证最多显示 5 秒）
- **AI 预设服务商 URL 拼接 bug**：修复通义/智谱/月之暗面预设配置开箱即用必 404 的问题（接口路径与 baseUrl 版本段重复）
- **AI 助手竞态**：修复发送中清空/新建对话导致消息丢失、进程重启恢复覆盖用户新消息的问题（统一任务管理 + 取消在途任务）
- **AI 并发控制**：AI 回复中重复触发学习工具被正确忽略（防重入）
- **RAG 降级**：资料库检索失败不再阻断 AI 回答（降级为无上下文调用）
- **提示词注入封堵**：苏格拉底引导阶段 2/3 的上一阶段输出加边界标记与注入警告

## [v0.9.22] - 2026-08-02

深度代码审查后的改进批 A + B（全仓库 3 路并行审查 + 关键问题人工复验）。

### 修复

- **底栏 double inset**：修复 COMPACT 布局底部手势区被双重消费导致的"底栏上方大面积空白"（外层只 pad 80dp，手势区交由内层 Scaffold 消费，与顶部 statusBars 处理对称）
- **主题保存错误提示**：设置页消费主题保存失败事件并弹 Snackbar（此前 DataStore 异常被静默丢弃）
- **FSRS 除零防御**：修复老数据 stability=0 时 `nextRecallStability` 产生 NaN 污染调度的问题
- **数据库 8→9 迁移**：为存量用户补建错题本两个复合索引（`[point_id, source]` / `[exam_question_id, source]`），提升错题查询性能
- **错题并发修复**：`recordWrongAnswer` 查找+递增/插入合并为单个事务，杜绝并发重复插入
- **时间源对齐**：错题记录改用 ClockGuard 时间源（与 FSRS 调度一致，避免时钟回拨时错乱）
- **评分防重入**：错题本 DUE 模式评分加防重入锁，避免连点导致 FSRS 重复调度

## [v0.9.21] - 2026-08-02

### 修复

- **MD3 规范底栏回归 + inset 双重消费修复**：底栏改为标准 Material 3 风格（surfaceContainer 实色 + 80dp + secondaryContainer 指示器），修复底栏/顶栏 inset 双重消费导致的布局空白
- **发布流程加固**：release.yml 支持同 tag 重建覆盖（`update_release: true`），修复 v0.9.20 已发布旧代码无法更新的问题

## [v0.9.20] - 2026-08-02

### 变更

- **底栏回归规范 MD3 风格**：从"流体玻璃/毛玻璃"回归标准 Material 3（surfaceContainer 实色 + 80dp + secondaryContainer 指示器），保留 scroll-aware 滚动感知显隐
- **滚动感知导航栏**：5 个顶级 Tab 支持下滑隐藏/上滑显示导航栏（spring 动画）

> 注：本版本因发布流程问题，实际发布的 APK 未包含随后的 inset 修复（修复已在 v0.9.21 完整发布）。

## [v0.9.19] - 2026-08-01

### 修复

- **紧凑玻璃导航栏**：悬浮导航栏高度 80dp→56dp，圆角 24dp，半透明玻璃质感，移除 BottomGradientScrim，解决导航栏+渐变遮罩合计约 184dp 占用过高的问题
- **种子加载超时重试**：超时 120s→300s + 1 次重试，防止低端设备首次加载超时导致知识点数据丢失

## [v0.9.18] - 2026-07-27

### 新增

- **错题本手动加入**：知识卡片可手动加入错题本
- **App 启动图标 v3**：印章文风格（米色印面 + 墨黑"文"字 + 圆角 12dp + monochrome 适配 Android 13+）

### 修复

- **日志门面统一**：20+ 文件迁移到 Timber 统一日志

## [v0.9.17] - 2026-07-27

### 修复

- **题号前缀剥离**：集中清洗所有题目内容的阿拉伯数字/中文数字题号前缀（6 个 UI 展示点统一处理）

## [v0.9.16] - 2026-07-27

### 变更

- **真题→论述题迁移**：底部导航第 2 个 Tab 从"真题"替换为"论述题"

## [v0.9.10] - 2026-07-31

### 修复

- **全面内容审计**：935 知识点 + 134 论述题内容完整性与学术准确性审计（0 个内容问题）
- 论述题详情分值显示修复、versionName 对齐

## [v0.9.9] - 2026-07-30

### 新增

- **论述题 AI 审题助手**：三阶段苏格拉底式引导（论证分析 → 改进建议 → 参考范文）+ 自评回写错题本

## [v0.9.8] - 2026-07-30

### 新增

- **论述题板块**：134 题三维筛选（年份/科目/审题思路）+ 详情页 10 区块结构 + 知识点关联

## [v0.9.5] - 2026-07-29

### 新增

- **时钟守卫**：检测系统时钟回拨，避免 FSRS 调度异常

## [v0.9.4] - 2026-07-29

### 新增

- **错题本 FSRS 调度**：错题支持间隔重复复习（10 个调度字段 + 待复习列表）

## [v0.9.3] - 2026-07-29

### 移除

- **知识图谱功能**：图谱 UI 及 core 层图谱设施移除，改走知识点树结构 + 关联模块

## [v0.9.0] - 2026-07-28

### 新增

- **知识图谱功能**（后于 v0.9.3 移除）
