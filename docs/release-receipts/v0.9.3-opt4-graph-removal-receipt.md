# v0.9.3 优化 4 移除图谱死代码 — Commit Receipt

**Date:** 2026-07-28
**Commit:** (pending — will be created after this receipt)
**Specialist:** agent-pr-review (per staff-engineer-mode Agent Event Policy)

## Intent
v0.9.0 删除 feature:graph UI 模块后，core 层图谱基础设施（GraphRepository/Impl + 3 算法服务 + GraphSkeleton + 2 DAO + 4 Entity）无任何消费者，每次启动 SeedDataLoader.importGraphSkeleton 生成 2123 节点 968 边纯死代码。删除这些设施 + DROP graph 表。

## Diff scope
- 28 文件变更：+1836 / -4063 行（净减少 2227 行）
- 删除 16 文件（graph 相关 source + test）
- 新增 2 文件（Migration_6_7.kt + schema 7.json）
- 修改 10 文件（清理 graph 引用：DataModule / DatabaseModule / SeedDataLoader / WenyanDatabase / consumer-rules.pro / CardRepository / ClockGuard / FlowExt / MemoRecordDao / Migration_3_4）

## Verification
- `:app:assembleDebug` SUCCESSFUL
- `:app:testDebugUnitTest` 386 tests, 0 failures, 0 errors
- Grep 确认全项目无 graph 设施引用（仅 Migration_6_7 注释）

## Verdict
✅ READY TO COMMIT

## Findings
- follow-up: STATUS.md 数据库版本 6→7 同步（含图谱数据层从"保留"改为"移除"）
- follow-up: SESSION_LOG.md 补 v0.9.3 条目

## Risk acceptance
- DROP TABLE 不可逆，但 graph 数据由 SeedDataLoader 自动生成（非用户数据），损失可接受
- Migration 顺序正确（graph_edges 子表先于 graph_nodes 父表）+ IF EXISTS 兜底

## Override
无 blocker / must-fix，可自主 commit。
